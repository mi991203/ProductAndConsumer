server:
  port: 7001
spring:
  application:
    name: eureka-server
#  security:
#    user:
#      name: admin
#      password: admin
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7001/eureka/
  instance:
    hostname: eureka
